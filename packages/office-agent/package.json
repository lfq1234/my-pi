{
  "name": "@earendil-works/pi-office-agent",
  "version": "0.0.3",
  "private": false,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "clean": "shx rm -rf dist",
    "build": "tsgo -p tsconfig.build.json",
    "build:bun": "bun build --compile --no-compile-autoload-bunfig ./src/bun/cli.ts --outfile ./dist/office-agent",
    "build:binary": "npm run build && bun build --compile --no-compile-autoload-bunfig ./src/bun/cli.ts --outfile ./dist/office-agent",
    "start": "node dist/cli.js",
    "start:bun": "bun run ./src/bun/cli.ts",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "office-agent",
    "agent",
    "office",
    "html",
    "email",
    "poster"
  ],
  "dependencies": {
    "jszip": "3.10.1"
  },
  "author": "Earendil Works",
  "license": "MIT",
  "engines": {
    "node": ">=22.19.0"
  }
}
